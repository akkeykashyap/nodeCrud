<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav">
            <a class="text-blue-500" href="/">back to home</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">All User</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(allusers.length> 0) { %>
                <% allusers.forEach(function(user){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 bg-zinc-500 rounded-lg overflow-hidden">
                            <img class="h-full w-full object-cover" src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-2xl tracking-tighter mt-3">
                            <%= user.name %>
                        </h3>
                        <h5 class="text-zinc-500">
                            <%= user.email %>
                        </h5>
                        <div class="flex gap-3">
                            <a class="text-blue-500" href="/edit/<%= user._id %>">Edit user</a>
                            <a class="text-blue-500" href="/delete/<%= user._id %>">Delete user</a>
                        </div>
                    </div>
                    <% }) %>
                        <%} else{ %>
                            <h3 class="text-zinc-500">no user found</h3>
                        <%}%>
                    </div>              
                 </div>
            <div>
        </div>
     </div>  
</body>

</html>